local prompt
if [[ -n $RAILS_ENV ]]; then
  if [[ $RAILS_ENV == 'development' ]]; then
    prompt='dev'
  elif [[ $RAILS_ENV == 'production' ]]; then
    prompt='prod'
  else
    prompt=$RAILS_ENV
  fi
  print $Z_LIGHT_YELLOW $prompt$Z_NO_COLOR
fi