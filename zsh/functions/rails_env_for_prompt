if [[ -n $RAILS_ENV ]]; then
  if [[ $RAILS_ENV == 'development' ]]; then
    print "${Z_LIGHT_YELLOW}dev${Z_NO_COLOR}"
  elif [[ $RAILS_ENV == 'production' ]]; then
    print "${Z_LIGHT_GREEN}pro${Z_NO_COLOR}"
  else
    print $RAILS_ENV
  fi
fi