" I prefer soft tabs but may have to switch as the Gophers prefer it.
"
" http://golang.org/doc/effective_go.html#formatting
setlocal noexpandtab autoindent shiftwidth=4 tabstop=4

" We don't need to highlight whitespace, as the ':Fmt' command will tidy
" everything up for us.
setlocal nolist
